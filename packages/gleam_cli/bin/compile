#!/usr/bin/env bash
 
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/.."

gleam build
bun build build/dev/javascript/eyg_cli/eyg_cli.mjs --compile --footer="main();" --outfile ./dist/eyg