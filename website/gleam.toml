name = "website"
version = "1.0.0"
target = "javascript"

# Fill out these fields if you intend to generate HTML documentation or publish
# your project to the Hex package manager.
#
# description = ""
# licences = ["Apache-2.0"]
# repository = { type = "github", user = "", repo = "" }
# links = [{ title = "Website", href = "" }]
#
# For a full reference of all the available options, you can have a look at
# https://gleam.run/writing-gleam/gleam-toml/.

[dependencies]
gleam_stdlib = ">= 0.34.0 and < 2.0.0"
plinth = ">= 0.5.5 and < 1.0.0"
gleam_javascript = ">= 0.13.0 and < 1.0.0"
snag = ">= 1.1.0 and < 2.0.0"
midas_node = ">= 1.2.2 and < 2.0.0"
midas = ">= 1.4.1 and < 2.0.0"
mysig = {path = "../../mysig"}
eyg = {path = "../eyg"}
gleam_json = ">= 2.1.0 and < 3.0.0"
lustre = ">= 4.6.3 and < 5.0.0"
midas_browser = ">= 0.1.2 and < 1.0.0"
supa = ">= 1.0.1 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
