# DNSimple

Follow the advice in ../api_clients.md.

The Open API Spec is available at https://developer.dnsimple.com/v2/openapi.yml

Test all GET requests against the live API.
Use `eyg_packages/spotless/index.eyg` to get a token.

Include all the operations for accounts, domains and billing charges
Include creating client configuration from and API token.
Include creating a client from OAuth using spotless.run.
Create a single `index.eyg`.