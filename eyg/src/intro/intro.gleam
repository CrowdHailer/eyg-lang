import intro/state
import intro/view/page
import lustre

pub fn run() {
  todo
  // let app = lustre.application(state.init, state.update, page.render)
  // let assert Ok(_) = lustre.start(app, "#app", Nil)
  Nil
}
