pub fn simple_server_test() {
    // Is there a way to have a harness pass around a state
    // handler, processes
    // eval_call offset [], []
    // get new and manually do somethning
    // Need to return run in handler so that we can send messages
    // deliver_messages
    // Run(value) <-
    // Then I want to send it somewhere having used the wrapping of an AST 
    // call(call(then, Run(handler)), value -> sendout value)
    // Can manually send at this point
    // send to the handler
    todo("how do I server here")
}